// 사용자 데이터 타입 정의
export interface UserData {
  userId: string;
  userName: string;
  imgNum: number; // 프로필 사진 번호
  mainDeviceId: number;
  date: string;
  weatherIcon: string;
  weatherDescription: string;
}

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.scentify.device.model.repository.DeviceRepository">
	<select id="selectDevicesByIds" parameterType="list" resultType="DeviceInfoDto">
	    SELECT *
	    FROM devices
	    WHERE id IN
	    <foreach item="id" collection="deviceIds" open="(" separator="," close=")">
	        #{id}
	    </foreach>
	</select>
</mapper>